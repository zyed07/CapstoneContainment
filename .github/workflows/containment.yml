name: Containment Demo

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run security scan
        run: echo "🔒 Scanning code for malicious patterns..."

      - name: Containment action
        if: always()
        run: echo "🚨 Malicious code detected → Disabling pipeline & notifying owners"
